<h1>List of Songs</h1>
<em><a href="/songs/new">Add a new song</a></em>

<ul>
    <% @songs.each do |song| %>
        <li><a href="/songs/<%= song.id %>">
            <%= song.name %>
        </a> by <b><%= song.artist.name %></b></li>
    <% end %>
</ul>
